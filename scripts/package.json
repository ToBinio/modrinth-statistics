{
	"name": "scripts",
	"private": true,
	"devDependencies": {
		"@types/node": "latest"
	},
	"dependencies": {
		"unstorage": "^1.16.1",
		"@upstash/redis": "^1.35.3"
	},
	"scripts": {
		"migrate": "node run migrate.ts",
		"db-fix": "node run db-fix.ts"
	}
}
